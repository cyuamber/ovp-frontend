const state = {
  isShow: false
}

const mutations = {
  setIsShow(state,bool){
    state.isShow = bool;
  }
}

const actions = {
  
}

const getters = {

}

export default {state, mutations, actions, getters, namespaced: true}